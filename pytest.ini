# pytest.ini
[pytest]
pythonpath = .
markers =
    slow: uses real embeddings; skipped by make test-fast
    relevance: opt-in public-corpus retrieval regression checks
filterwarnings =
    ignore:builtin type.*has no __module__:DeprecationWarning
